# v0.2.0

## Highlights
- 补录历史 `v0.2.0` tag 的发布说明，使 release state 审计能够完整关联本地 tag 与 release notes。

## Fixes
- 修复 `v0.2.0` 被 `release-sync` / `workflow-seal` 识别为 orphan tag 的治理缺口。

## Verification
- [x] ./scripts/windows/task.ps1 preflight
- [x] CI passed
- [x] Release assets verified (exe/setup/checksums)

